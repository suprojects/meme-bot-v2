[pyrogram]
api_id = {api_id}
api_hash = {api_hash}

[tokens]
api_token = {api_token}

[plugins]
root = handlers

[database]
uri = {uri}
db_name = {db_name}

[sudo]
users = {sudo_users}
chat = {sudo_chat}
log = {log_chat}

[imgflip]
url = https://api.imgflip.com/caption_image
username = {imgflip_username}
password = {imgflip_password}